---
tags: [projects]
allowed_tools: [Read, Glob, Grep, Skill]
---

Walk me through creating a new Mockfly mock API project called
"demo-api" with one GET endpoint at /status that returns {"ok": true}.
Don't actually run any commands — just tell me the plan.
